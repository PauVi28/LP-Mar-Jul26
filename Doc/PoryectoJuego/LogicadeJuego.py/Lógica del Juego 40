from cards import crear_mazo
import random

class Game40:

    def _init_(self):

        self.mazo = crear_mazo()

        self.jugador1 = []
        self.jugador2 = []

        self.mesa = []

        self.turno = 1

        self.puntos_j1 = 0
        self.puntos_j2 = 0

        self.estado = "MENU"

    def repartir_cartas(self):

        random.shuffle(self.mazo)

        self.jugador1 = self.mazo[:5]
        self.jugador2 = self.mazo[5:10]

        self.mesa = self.mazo[10:14]

    def cambiar_turno(self):

        if self.turno == 1:
            self.turno = 2
        else:
            self.turno = 1

    def validar_jugada(self, carta):

        if carta is None:
            return False

        return True

    def calcular_puntos(self):

        pass

    def verificar_ganador(self):

        if self.puntos_j1 >= 40:
            return "Jugador 1"

        if self.puntos_j2 >= 40:
            return "Jugador 2"

        return None

Nota: Este es el inico del trabajo y puede ser modificado dependiendo el requerimiento del trabajado.
